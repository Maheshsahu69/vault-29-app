import React from 'react';
import { 
    IonApp, 
    IonImg,
    IonContent
 } from '@ionic/react';

const LoginPage: React.FC=()=> {
return (
   <IonApp>
       <IonContent>
          <div>Login Page</div>
       </IonContent>
   </IonApp>
);
};

export default LoginPage;